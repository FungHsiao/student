<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yt">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".StudentActivity" />
        <activity android:name=".TeacherActivity" />
        <activity android:name=".InsertActivity" />
        <activity android:name=".QueryActivity" />
        <activity android:name=".UpdateActivity" />
        <activity android:name=".DeleteActivity" />
        <activity android:name=".deleteDialog" />
        <activity android:name=".StuQueryActivity" />
        <activity android:name=".delete_one_dialogActivity" />
        <activity android:name=".UpdateDialogActivity" />
        <activity android:name=".showActivity"></activity>
    </application>

</manifest>